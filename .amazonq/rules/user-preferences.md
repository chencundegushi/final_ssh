# 用户偏好设置

## 响应要求
- 收到人类的请求时先使用工具`sequentialthinking`思考
- 使用中文回复
- 代码需严谨规范
- Windows环境，避免使用`&&`操作符
- 专注代码生成和优化，忽略现有导包错误
- 代码的log使用英文，注释则使用中文

## 修改文件前必须
- 用 listDirectory 确认文件路径
- 使用完整绝对路径，格式：e:/path/to/file.ts
- Windows系统用正斜杠 `/`，避免反斜杠 `\`
- 修改文件时避免改到无相关的代码
